// tailwind.config.js (example)
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.html",
    "./src/**/*.ts", // If you use classes in TS/JS files (e.g., for dynamic class names)
    // Add other paths as needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}